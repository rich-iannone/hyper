#' Create a set of CSS rules
#'
#' Create a set of CSS rules that can be added
#' to an HTML document or written to a file.
#' @param selectors the selector or set of selectors
#' to use for the CSS rule set.
#' @param rules a list of rules to use in the rule
#' set's declaration block.
#' @export
create_css_rules <- function(selectors,
                             rules = NULL) {

  NULL
}
